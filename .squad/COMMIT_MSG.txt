fix: blank inference output and implement font merge feature

Major: Fixed blank preview glyphs by extracting pathname from manifest's
absolute URL before passing to web worker. Web workers bypass Vite proxy,
so they need relative paths. Added debug logging to inferenceWorker.ts.
Fixed TypeScript error in FontAssembler.ts (glyph.name can be null).

Togusa: Implemented font merge feature to combine AI-generated Cyrillic
glyphs with uploaded fonts. FontAssembler now accepts uploadedFont
parameter and merges non-Cyrillic glyphs from the original. Added UPM
scaling to GlyphVectorizer to match uploaded font metrics. 3 new tests
added. All 111 tests passing.

Decisions merged to .squad/decisions.md.
Cross-agent updates appended to agent history files.
Orchestration logs: 2026-03-08T193433Z-{major,togusa}.md
Session log: 2026-03-08T193433Z-blank-glyph-fix-font-merge.md

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
