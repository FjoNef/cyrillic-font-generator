# Session Log: PR #4 Color Inversion Fix — 2026-02-25T152319

Major fixed the blocking color inversion bug in PR #4. Model output mapping formula corrected from `((output[px] + 1) / 2) * 255` to `((1 - output[px]) / 2) * 255` in App.tsx and OnnxInference.ts. Fixes inverted glyph rendering (black backgrounds → white backgrounds with black ink). Pushed to feat/togusa-inference-pipeline; PR ready for re-review.
