# Orchestration Log: Major (AI/ML Engineer)

**Timestamp:** 2026-03-05T00:00:48Z  
**Agent:** Major  
**Task:** Evaluated base_filters 64→32 quality/size/training tradeoff

## Summary

Major completed analysis of reducing UNetGenerator `base_filters` from 64 → 32, concluding that nf=32 is sufficient to achieve the 20MB browser delivery target with INT8+brotli compression while saving 8-9 hours of training time.

## Key Findings

- **Size reduction:** 2.80× overall (60.3M → 21.6M params). nf=32 INT8 ≈ 23 MB → ~17-20 MB with brotli delivery.
- **Quality:** Minimal impact. StyleEncoder unchanged; decoder capacity at nf=32 ample for 128×128 near-binary glyphs.
- **Training savings:** ~8-9 hours (200 epochs at nf=32: 10-13h vs. remaining 177 at nf=64: 20.7h).

## Recommendation

**Stop current nf=64 training at epoch 23, switch to nf=32, retrain from scratch.**

Rationale: Low sunk cost (~2.7h at epoch 23/200), significant time savings, negligible quality loss, no inference contract changes.

## Required Actions

1. Update `src/model/train/train.py` to pass `base_filters=32` at UNetGenerator init
2. Update `src/model/export/export_onnx.py` line 83 to `base_filters=32`
3. Discard epoch_0020.pth checkpoint (incompatible state_dict shapes)
4. Retrain: `python src/model/train/train.py --config ... --num_epochs 200`
5. Confirm with Batou: ASP.NET Core serves `.onnx` with brotli compression on `/api/model`

## Output Files

- Decision doc: `.squad/decisions/inbox/major-base-filters-tradeoff.md`
- History updated: `.squad/agents/major/history.md`

## Status

RECOMMENDED — Awaiting FjoNef approval to stop current training run and proceed with retraining at nf=32.
